import styles from './PurchasesHeading.module.css';

export default function PurchasesHeading() {
    return (
        <div className={styles.wrapper}>
            <p className={styles.heading}>Purchases</p>
        </div>
    );
};
